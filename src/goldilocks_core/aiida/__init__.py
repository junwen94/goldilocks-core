"""AiiDA integration for goldilocks-core.

Install the optional extra to use this module::

    pip install goldilocks-core[aiida]

The sub-modules guard against missing aiida-core at import time so the
package itself can be imported freely.
"""
